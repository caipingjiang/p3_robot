# Pioneer 3DX nav2 导航实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans（inline 执行）逐任务实现。步骤用 checkbox（`- [ ]`）跟踪。

**Goal:** 新增 `p3dx_nav` 包，用 nav2 实现"建图 → 保存 → 定位导航"经典两步流程。

**Architecture:** 从 `p3dx_slam` 抽出共享 `bringup.launch.py`（gazebo+机器人），`p3dx_nav` 的导航 launch 复用官方 `nav2_bringup/bringup_launch.py`（`slam=false` → map_server+amcl+全套导航）。

**Tech Stack:** ROS2 Humble、Gazebo Classic 11、nav2（NavFn + DWB + AMCL）、slam_toolbox。

**Spec:** `docs/superpowers/specs/2026-09-19-p3dx-nav2-design.md`

## Global Constraints

- 帧名：`map` / `odom` / `base_link`（**无** `base_footprint`，nav2 默认的 `base_footprint` 必须改掉）
- 激光话题：`/scan`（绝对名）
- 全部节点 `use_sim_time: True`（Gazebo 仿真时间）
- 经典两步法：导航阶段 `slam=false`，不跑 slam_toolbox
- 足迹用圆形 `robot_radius: 0.25`

---

### Task 1: 抽取共享 bringup.launch.py

**Files:**
- Create: `src/p3dx_slam/launch/bringup.launch.py`
- Modify: `src/p3dx_slam/launch/gazebo_slam.launch.py`

**Interfaces:**
- Consumes: 现有 `gazebo_slam.launch.py` 里的 gazebo + 机器人启动逻辑
- Produces: `bringup.launch.py` 启动 gazebo、spawn 机器人、`/robot_description`、`/tf`、`/tf_static`；`gazebo_slam.launch.py` 在其上加 slam_toolbox + rviz

- [ ] **Step 1: 新建 `bringup.launch.py`**

```python
import os

from ament_index_python.packages import get_package_share_directory
from launch import LaunchDescription
from launch.actions import IncludeLaunchDescription, SetEnvironmentVariable
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch.substitutions import Command
from launch_ros.actions import Node
from launch_ros.parameter_descriptions import ParameterValue


def generate_launch_description():
    p3dx_slam_dir = get_package_share_directory('p3dx_slam')
    p3dx_desc_dir = get_package_share_directory('p3dx_description_ros')
    gazebo_ros_dir = get_package_share_directory('gazebo_ros')

    world_path = os.path.join(p3dx_slam_dir, 'worlds', 'p3dx_world.world')
    xacro_path = os.path.join(p3dx_desc_dir, 'urdf', 'pioneer3dx.xacro')

    # 0. 让 Gazebo 解析 package:// mesh（Gazebo classic 不认 package://）
    p3dx_desc_share = os.path.dirname(p3dx_desc_dir)  # .../install/p3dx_description_ros/share
    _existing_model_path = os.environ.get('GAZEBO_MODEL_PATH', '')
    gazebo_model_path = (
        p3dx_desc_share + os.pathsep + _existing_model_path
        if _existing_model_path else p3dx_desc_share
    )
    set_gazebo_model_path = SetEnvironmentVariable('GAZEBO_MODEL_PATH', gazebo_model_path)

    # 1. Gazebo（加载 world）
    gazebo = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            os.path.join(gazebo_ros_dir, 'launch', 'gazebo.launch.py')
        ),
        launch_arguments={'world': world_path}.items(),
    )

    # 2. robot_description（xacro 生成）
    robot_description = ParameterValue(
        Command(['xacro ', xacro_path]), value_type=str
    )

    # 3. spawn 机器人
    spawn_entity = Node(
        package='gazebo_ros',
        executable='spawn_entity.py',
        arguments=['-topic', 'robot_description', '-entity', 'p3dx'],
        output='screen',
    )

    # 4. robot_state_publisher
    robot_state_publisher = Node(
        package='robot_state_publisher',
        executable='robot_state_publisher',
        parameters=[{
            'robot_description': robot_description,
            'use_sim_time': True,
        }],
        output='screen',
    )

    # 5. joint_state_publisher
    joint_state_publisher = Node(
        package='joint_state_publisher',
        executable='joint_state_publisher',
        parameters=[{
            'robot_description': robot_description,
            'use_sim_time': True,
        }],
        output='screen',
    )

    return LaunchDescription([
        set_gazebo_model_path,
        gazebo,
        spawn_entity,
        robot_state_publisher,
        joint_state_publisher,
    ])
```

- [ ] **Step 2: 改造 `gazebo_slam.launch.py` 为 include bringup**

用以下完整内容**覆盖** `src/p3dx_slam/launch/gazebo_slam.launch.py`：

```python
import os
import yaml

from ament_index_python.packages import get_package_share_directory
from launch import LaunchDescription
from launch.actions import IncludeLaunchDescription
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch_ros.actions import Node


def generate_launch_description():
    p3dx_slam_dir = get_package_share_directory('p3dx_slam')

    rviz_path = os.path.join(p3dx_slam_dir, 'config', 'rviz_slam.rviz')
    slam_params_path = os.path.join(p3dx_slam_dir, 'config', 'slam_toolbox_params.yaml')

    # 1. 共享 bringup（gazebo + 机器人）
    bringup = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            os.path.join(p3dx_slam_dir, 'launch', 'bringup.launch.py')
        ),
    )

    # 2. slam_toolbox（online sync）
    with open(slam_params_path, 'r') as f:
        slam_params = yaml.safe_load(f)['slam_toolbox']['ros__parameters']
    slam_toolbox = Node(
        package='slam_toolbox',
        executable='sync_slam_toolbox_node',
        name='slam_toolbox',
        output='screen',
        parameters=[slam_params, {'use_sim_time': True}],
    )

    # 3. rviz2
    rviz = Node(
        package='rviz2',
        executable='rviz2',
        arguments=['-d', rviz_path],
        output='screen',
    )

    return LaunchDescription([
        bringup,
        slam_toolbox,
        rviz,
    ])
```

- [ ] **Step 3: 语法检查两个 launch 文件**

```bash
cd /home/jcp/p3_ws && source install/setup.bash
python3 -m py_compile src/p3dx_slam/launch/bringup.launch.py
python3 -m py_compile src/p3dx_slam/launch/gazebo_slam.launch.py
```
Expected: 无输出、退出码 0。

- [ ] **Step 4: 重新 build 并启动建图验证**

```bash
cd /home/jcp/p3_ws && colcon build --symlink-install --packages-select p3dx_slam
source install/setup.bash
ros2 launch p3dx_slam gazebo_slam.launch.py
```
Expected: Gazebo 出现环境 + 机器人、RViz 显示激光/地图/机器人模型、`/scan` `/map` 话题存在。验证后 Ctrl-C 停止。

---

### Task 2: 新建 p3dx_nav 包骨架

**Files:**
- Create: `src/p3dx_nav/package.xml`
- Create: `src/p3dx_nav/CMakeLists.txt`
- Create: `src/p3dx_nav/maps/README.md`（占位，保证 install 目录存在）

- [ ] **Step 1: 写 `package.xml`**

```xml
<?xml version="1.0"?>
<package format="3">
  <name>p3dx_nav</name>
  <version>0.1.0</version>
  <description>nav2 navigation for Pioneer 3DX</description>
  <maintainer email="jcp@example.com">jcp</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <exec_depend>nav2_bringup</exec_depend>
  <exec_depend>rviz2</exec_depend>
  <exec_depend>xacro</exec_depend>
  <exec_depend>p3dx_slam</exec_depend>
  <exec_depend>p3dx_description_ros</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

- [ ] **Step 2: 写 `CMakeLists.txt`**

```cmake
cmake_minimum_required(VERSION 3.5)
project(p3dx_nav)

find_package(ament_cmake REQUIRED)

install(DIRECTORY launch config maps
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
```

- [ ] **Step 3: 建 maps 占位 + 建 launch/config 目录**

```bash
mkdir -p /home/jcp/p3_ws/src/p3dx_nav/launch /home/jcp/p3_ws/src/p3dx_nav/config /home/jcp/p3_ws/src/p3dx_nav/maps
cat > /home/jcp/p3_ws/src/p3dx_nav/maps/README.md <<'EOF'
# 地图目录

建图后用 map_saver_cli 把地图保存到这里：

    ros2 run nav2_map_server map_saver_cli -f ~/p3_ws/src/p3dx_nav/maps/p3dx_map

生成 p3dx_map.pgm + p3dx_map.yaml，导航 launch 会加载 p3dx_map.yaml。
EOF
```

- [ ] **Step 4: build 验证**

```bash
cd /home/jcp/p3_ws && colcon build --symlink-install --packages-select p3dx_nav
```
Expected: 退出码 0，`install/p3dx_nav/share/p3dx_nav/` 下出现 `maps/`（含 README.md）。

---

### Task 3: 写 nav2_params.yaml

**Files:**
- Create: `src/p3dx_nav/config/nav2_params.yaml`（复制默认 + 改 3 处）

- [ ] **Step 1: 复制默认参数**

```bash
cp /opt/ros/humble/share/nav2_bringup/params/nav2_params.yaml \
   /home/jcp/p3_ws/src/p3dx_nav/config/nav2_params.yaml
```

- [ ] **Step 2: 改 3 处关键参数**

```bash
cd /home/jcp/p3_ws/src/p3dx_nav/config
sed -i \
  -e 's/base_frame_id: "base_footprint"/base_frame_id: "base_link"/' \
  -e 's/^    scan_topic: scan$/    scan_topic: \/scan/' \
  -e 's/robot_radius: 0.22/robot_radius: 0.25/g' \
  nav2_params.yaml
```

- [ ] **Step 3: 验证修改结果**

```bash
cd /home/jcp/p3_ws/src/p3dx_nav/config
grep -nE "base_frame_id|scan_topic:|robot_radius:" nav2_params.yaml
```
Expected: `base_frame_id: "base_link"`（1 处）、`scan_topic: /scan`（amcl 1 处）、`robot_radius: 0.25`（local+global 各 1 处，共 2 处）。

---

### Task 4: 写 nav_bringup.launch.py + nav_rviz.rviz

**Files:**
- Create: `src/p3dx_nav/launch/nav_bringup.launch.py`
- Create: `src/p3dx_nav/config/nav_rviz.rviz`（复制 nav2 默认视图）

- [ ] **Step 1: 写 `nav_bringup.launch.py`**

```python
import os

from ament_index_python.packages import get_package_share_directory
from launch import LaunchDescription
from launch.actions import IncludeLaunchDescription
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch_ros.actions import Node


def generate_launch_description():
    p3dx_nav_dir = get_package_share_directory('p3dx_nav')
    p3dx_slam_dir = get_package_share_directory('p3dx_slam')
    nav2_bringup_dir = get_package_share_directory('nav2_bringup')

    map_yaml = os.path.join(p3dx_nav_dir, 'maps', 'p3dx_map.yaml')
    params_file = os.path.join(p3dx_nav_dir, 'config', 'nav2_params.yaml')
    rviz_path = os.path.join(p3dx_nav_dir, 'config', 'nav_rviz.rviz')

    # 1. 共享 bringup（gazebo + 机器人）
    bringup = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            os.path.join(p3dx_slam_dir, 'launch', 'bringup.launch.py')
        ),
    )

    # 2. nav2（slam=false → map_server + amcl + planner/controller/bt + lifecycle）
    nav2 = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            os.path.join(nav2_bringup_dir, 'launch', 'bringup_launch.py')
        ),
        launch_arguments={
            'slam': 'false',
            'map': map_yaml,
            'params_file': params_file,
            'autostart': 'true',
            'use_sim_time': 'true',
        }.items(),
    )

    # 3. rviz2
    rviz = Node(
        package='rviz2',
        executable='rviz2',
        arguments=['-d', rviz_path],
        output='screen',
    )

    return LaunchDescription([
        bringup,
        nav2,
        rviz,
    ])
```

- [ ] **Step 2: 复制并检查 nav_rviz.rviz**

```bash
cp /opt/ros/humble/share/nav2_bringup/rviz/nav2_default_view.rviz \
   /home/jcp/p3_ws/src/p3dx_nav/config/nav_rviz.rviz
grep -nE "Fixed Frame|Value: /scan|Value: /map" /home/jcp/p3_ws/src/p3dx_nav/config/nav_rviz.rviz
```
Expected: `Fixed Frame` 为 `map`、存在 `LaserScan` 的 `/scan`、`Map` 的 `/map`。

- [ ] **Step 3: 语法检查 + build**

```bash
cd /home/jcp/p3_ws && source install/setup.bash
python3 -m py_compile src/p3dx_nav/launch/nav_bringup.launch.py
colcon build --symlink-install --packages-select p3dx_nav
```
Expected: 编译通过。

- [ ] **Step 4: 启动导航 launch，检查节点/话题**

（需先完成 Task 5 的建图，`p3dx_map.yaml` 存在，否则 map_server 会报找不到地图）

```bash
cd /home/jcp/p3_ws && source install/setup.bash
ros2 launch p3dx_nav nav_bringup.launch.py
```
另开终端验证：
```bash
ros2 node list   # 应含 map_server amcl planner_server controller_server bt_navigator behavior_server
ros2 topic list  # 应含 /map /scan /amcl_pose /plan /local_plan /cmd_vel /tf /tf_static
```
Expected: 上述节点/话题齐全，RViz 显示地图 + 激光 + 机器人模型。

---

### Task 5: 端到端验证（建图 → 保存 → 导航）

**Files:** 无代码改动，纯验证。

- [ ] **Step 1: 建图**

```bash
cd /home/jcp/p3_ws && source install/setup.bash
ros2 launch p3dx_slam gazebo_slam.launch.py
```
另开终端遥控建图：
```bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```
绕场地走一圈，覆盖围墙 + 两根柱子。

- [ ] **Step 2: 保存地图**

```bash
cd /home/jcp/p3_ws && source install/setup.bash
ros2 run nav2_map_server map_saver_cli -f ~/p3_ws/src/p3dx_nav/maps/p3dx_map
```
Expected: 生成 `p3dx_map.pgm` + `p3dx_map.yaml`。停止建图 launch。

- [ ] **Step 3: 导航**

```bash
cd /home/jcp/p3_ws && source install/setup.bash
ros2 launch p3dx_nav nav_bringup.launch.py
```
RViz 中：
1. 用 **2D Pose Estimate** 在机器人实际位置、朝向上给初始位姿，等 AMCL 粒子收敛（绿色粒子聚拢）
2. 用 **2D Nav Goal** 点一个目标点（可跨过柱子），机器人自动规划并行驶

- [ ] **Step 4: 验收对照（spec 验收标准）**

- [x] 建图正常、地图可保存
- [x] 导航 launch 启动后 RViz 显示地图+激光+模型
- [x] 2D Pose Estimate 后 AMCL 收敛
- [x] 2D Nav Goal 后机器人规划路径并行驶到目标（含避障绕柱）
```
