# p3dx_slam 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立 `p3dx_slam` 功能包，用 slam_toolbox 在 Gazebo 仿真中实现 Pioneer 3DX 的定位建图基本功能。

**Architecture:** 复用 `p3dx_description_ros` 的 URDF 几何，把它 ROS1 时代的 gazebo 插件配置 ROS2 化（diff_drive + ray_sensor），在 `p3dx_slam` 包里用 launch 串起 gazebo + spawn + rsp + jsp + slam_toolbox + rviz。TF 命名对齐 nav2 惯例（map→odom→base_link→lms100）。

**Tech Stack:** ROS2 Humble, Gazebo 11 (classic), gazebo_ros_pkgs 3.9.0, slam_toolbox, xacro, rviz2。

**Spec:** `docs/2026-09-19-p3dx-slam-design.md`

## Global Constraints

- ROS2 Humble + Gazebo 11（classic gazebo），不是 ros_gz/Ignition。
- 差速驱动用 `gazebo_ros_diff_drive` 插件，**不用** ros2_control。
- 话题命名（全局，无 namespace）：`/scan`、`/cmd_vel`、`/odom`、`/map`。
- frame 命名：`map` / `odom` / `base_link` / `lms100`（对齐 nav2）。
- 仿真节点全部 `use_sim_time: true`。
- 本期**不含** nav2 骨架。
- git 情况：`/home/jcp/p3_ws` 不是 git 仓库；`src/p3dx_description_ros` 是独立 git 仓库。Task 1 的 commit 在后者仓库内做；`p3dx_slam` 为新包，提交步骤跳过（由用户后续决定是否 `git init`）。
- 本计划无 pytest 单测；每个任务的「测试」是运行命令并核对输出（xacro 解析、colcon build、ros2 topic/tf 检查）。

---

## File Structure

```
p3dx_description_ros/                # 现有包（git 仓库）—— Task 1 改造
├── urdf/pioneer3dx.gazebo           # 重写为 ROS2 插件配置
└── urdf/pioneer3dx_wheel.xacro      # 移除 transmission（ros1 遗留）

p3dx_slam/                           # 新包 —— Task 2~5 新建
├── package.xml
├── CMakeLists.txt
├── launch/gazebo_slam.launch.py
├── config/slam_toolbox_params.yaml
├── config/rviz_slam.rviz
└── worlds/p3dx_world.world
```

---

## Task 1: 描述包 gazebo 配置 ROS2 化

**Files:**
- Modify: `src/p3dx_description_ros/urdf/pioneer3dx.gazebo`（整体重写）
- Modify: `src/p3dx_description_ros/urdf/pioneer3dx_wheel.xacro`（删除 transmission 块）

**Interfaces:**
- Consumes: 无（第一个任务）
- Produces: 可被 xacro 解析的 `pioneer3dx.xacro`，输出含 `libgazebo_ros_diff_drive.so` 与 `libgazebo_ros_ray_sensor.so` 插件，关节 `base_left_wheel_joint`/`base_right_wheel_joint` 为 continuous。

- [ ] **Step 1: 重写 pioneer3dx.gazebo**

用以下完整内容替换 `src/p3dx_description_ros/urdf/pioneer3dx.gazebo`：

```xml
<?xml version="1.0"?>

<!--
ROS2 (Humble) gazebo 插件配置：Pioneer 3DX
- 差速驱动：gazebo_ros_diff_drive（发布 odom->base_link，订阅 /cmd_vel）
- 激光雷达：gazebo_ros_ray_sensor（发布 /scan，frame lms100）
注意：不用 ${ns} 这类 xacro property（旧配置因此报 "name 'ns' is not defined"）。
-->

<robot xmlns:xacro="http://www.ros.org/wiki/xacro">

  <!-- 差速驱动 -->
  <gazebo>
    <plugin name="diff_drive" filename="libgazebo_ros_diff_drive.so">
      <update_rate>100</update_rate>
      <left_joint>base_left_wheel_joint</left_joint>
      <right_joint>base_right_wheel_joint</right_joint>
      <wheel_separation>0.39</wheel_separation>
      <wheel_diameter>0.15</wheel_diameter>
      <max_wheel_torque>5</max_wheel_torque>
      <max_wheel_acceleration>1.0</max_wheel_acceleration>
      <publish_odom>true</publish_odom>
      <publish_odom_tf>true</publish_odom_tf>
      <publish_wheel_tf>false</publish_wheel_tf>
      <odometry_frame>odom</odometry_frame>
      <robot_base_frame>base_link</robot_base_frame>
    </plugin>
  </gazebo>

  <!-- 激光雷达 lms100 -->
  <gazebo reference="lms100">
    <sensor type="ray" name="hokuyo_sensor">
      <pose>0 0 0 0 0 0</pose>
      <visualize>false</visualize>
      <update_rate>50</update_rate>
      <ray>
        <scan>
          <horizontal>
            <samples>360</samples>
            <resolution>1</resolution>
            <min_angle>-1.570796</min_angle>
            <max_angle>1.570796</max_angle>
          </horizontal>
        </scan>
        <range>
          <min>0.10</min>
          <max>30.0</max>
          <resolution>0.01</resolution>
        </range>
        <noise>
          <type>gaussian</type>
          <mean>0.0</mean>
          <stddev>0.01</stddev>
        </noise>
      </ray>
      <plugin name="hokuyo_controller" filename="libgazebo_ros_ray_sensor.so">
        <ros>
          <remapping>~/out:=scan</remapping>
        </ros>
        <output_type>sensor_msgs/LaserScan</output_type>
        <frame_name>lms100</frame_name>
      </plugin>
    </sensor>
  </gazebo>

  <!-- 材质 -->
  <gazebo reference="base_link">
    <material>Gazebo/Red</material>
  </gazebo>
  <gazebo reference="top_plate">
    <material>Gazebo/Black</material>
  </gazebo>
  <gazebo reference="swivel">
    <material>Gazebo/Grey</material>
  </gazebo>
  <gazebo reference="center_hubcap">
    <material>Gazebo/Grey</material>
  </gazebo>
  <gazebo reference="center_wheel">
    <material>Gazebo/Black</material>
    <mu1>10.0</mu1>
    <mu2>10.0</mu2>
    <kp>1000000.0</kp>
    <kd>1.0</kd>
  </gazebo>
  <gazebo reference="front_sonar">
    <material>Gazebo/Yellow</material>
  </gazebo>

</robot>
```

- [ ] **Step 2: 删除 pioneer3dx_wheel.xacro 里的 transmission 块**

在 `src/p3dx_description_ros/urdf/pioneer3dx_wheel.xacro` 中，删除从 `<transmission name="${parent}_${suffix}_wheel_trans">` 到其配对 `</transmission>` 的整块（含 `<type>`、`<joint>`、`<actuator>` 内容）。删除后该文件末尾的 `<gazebo reference="...">` 材质块保持不变。

- [ ] **Step 3: 验证 xacro 能解析并含 ROS2 插件**

Run:
```bash
source /opt/ros/humble/setup.bash && source /home/jcp/p3_ws/install/setup.bash
colcon build --packages-select p3dx_description_ros --symlink-install
xacro /home/jcp/p3_ws/install/p3dx_description_ros/share/p3dx_description_ros/urdf/pioneer3dx.xacro > /tmp/p3dx_full.xml
grep -o 'filename="libgazebo[^"]*"' /tmp/p3dx_full.xml | sort -u
```

Expected: 退出码 0，无 `name 'ns' is not defined`；grep 输出仅含 `libgazebo_ros_diff_drive.so` 和 `libgazebo_ros_ray_sensor.so`（不再有 `libgazebo_ros_laser.so` / `libgazebo_ros_p3d.so` / `libgazebo_ros_control.so`）。

- [ ] **Step 4: 提交（在 p3dx_description_ros 仓库内）**

```bash
cd /home/jcp/p3_ws/src/p3dx_description_ros
git add urdf/pioneer3dx.gazebo urdf/pioneer3dx_wheel.xacro
git commit -m "refactor: ROS2-ify gazebo plugins (diff_drive + ray_sensor)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

## Task 2: 创建 p3dx_slam 包骨架

**Files:**
- Create: `src/p3dx_slam/package.xml`
- Create: `src/p3dx_slam/CMakeLists.txt`

**Interfaces:**
- Consumes: 无
- Produces: 名为 `p3dx_slam` 的 ament_cmake 包，install 出 `share/p3dx_slam` 下的 `launch`/`config`/`worlds` 目录（供 Task 3~5 的文件落入）。

- [ ] **Step 1: 写 package.xml**

```xml
<?xml version="1.0"?>
<package format="3">
  <name>p3dx_slam</name>
  <version>0.1.0</version>
  <description>SLAM (slam_toolbox) and Gazebo simulation for Pioneer 3DX</description>
  <maintainer email="jcp@localhost">jcp</maintainer>
  <license>BSD-3-Clause</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <exec_depend>gazebo_ros</exec_depend>
  <exec_depend>robot_state_publisher</exec_depend>
  <exec_depend>joint_state_publisher</exec_depend>
  <exec_depend>slam_toolbox</exec_depend>
  <exec_depend>rviz2</exec_depend>
  <exec_depend>xacro</exec_depend>
  <exec_depend>p3dx_description_ros</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

- [ ] **Step 2: 写 CMakeLists.txt**

```cmake
cmake_minimum_required(VERSION 3.5)
project(p3dx_slam)

find_package(ament_cmake REQUIRED)

install(DIRECTORY launch config worlds
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
```

- [ ] **Step 3: 验证 build**

Run:
```bash
source /opt/ros/humble/setup.bash && source /home/jcp/p3_ws/install/setup.bash
cd /home/jcp/p3_ws && colcon build --packages-select p3dx_slam --symlink-install
ros2 pkg prefix p3dx_slam
```

Expected: build 成功，`ros2 pkg prefix p3dx_slam` 输出 `/home/jcp/p3_ws/install/p3dx_slam`。

- [ ] **Step 4: 提交**

`p3dx_slam` 位于非 git 的 `/home/jcp/p3_ws` 下，跳过 commit（若需纳入版本管理，用户后续在 `src/p3dx_slam` 下 `git init`）。

---

## Task 3: gazebo world 文件

**Files:**
- Create: `src/p3dx_slam/worlds/p3dx_world.world`

**Interfaces:**
- Consumes: 无
- Produces: 一个可被 `gazebo_ros` launch 的 `gazebo.launch.py` 通过 `world:=` 参数加载的世界，含地面、灯光、四面围墙、几个障碍物。

- [ ] **Step 1: 写 p3dx_world.world**

```xml
<?xml version="1.0"?>
<sdf version="1.6">
  <world name="p3dx_world">

    <!-- 地面 + 太阳 -->
    <include>
      <uri>model://ground_plane</uri>
    </include>
    <include>
      <uri>model://sun</uri>
    </include>

    <!-- 环境光 -->
    <light name="world_light" type="directional">
      <pose>0 0 10 0 0 0</pose>
      <diffuse>0.8 0.8 0.8 1</diffuse>
      <specular>0.2 0.2 0.2 1</specular>
      <direction>0 0 -1</direction>
    </light>

    <!-- 四面围墙（10m x 10m 场地，墙高 1m 厚 0.2m） -->
    <model name="wall_north">
      <static>true</static>
      <link name="link">
        <collision name="collision">
          <geometry><box><size>10 0.2 1</size></box></geometry>
        </collision>
        <visual name="visual">
          <geometry><box><size>10 0.2 1</size></box></geometry>
          <material><ambient>0.6 0.6 0.6 1</ambient></material>
        </visual>
      </link>
      <pose>0 5 0.5 0 0 0</pose>
    </model>

    <model name="wall_south">
      <static>true</static>
      <link name="link">
        <collision name="collision">
          <geometry><box><size>10 0.2 1</size></box></geometry>
        </collision>
        <visual name="visual">
          <geometry><box><size>10 0.2 1</size></box></geometry>
          <material><ambient>0.6 0.6 0.6 1</ambient></material>
        </visual>
      </link>
      <pose>0 -5 0.5 0 0 0</pose>
    </model>

    <model name="wall_east">
      <static>true</static>
      <link name="link">
        <collision name="collision">
          <geometry><box><size>0.2 10 1</size></box></geometry>
        </collision>
        <visual name="visual">
          <geometry><box><size>0.2 10 1</size></box></geometry>
          <material><ambient>0.6 0.6 0.6 1</ambient></material>
        </visual>
      </link>
      <pose>5 0 0.5 0 0 0</pose>
    </model>

    <model name="wall_west">
      <static>true</static>
      <link name="link">
        <collision name="collision">
          <geometry><box><size>0.2 10 1</size></box></geometry>
        </collision>
        <visual name="visual">
          <geometry><box><size>0.2 10 1</size></box></geometry>
          <material><ambient>0.6 0.6 0.6 1</ambient></material>
        </visual>
      </link>
      <pose>-5 0 0.5 0 0 0</pose>
    </model>

    <!-- 场内障碍物：两根圆柱 -->
    <model name="pillar_a">
      <static>true</static>
      <link name="link">
        <collision name="collision">
          <geometry><cylinder><radius>0.15</radius><length>1</length></cylinder></geometry>
        </collision>
        <visual name="visual">
          <geometry><cylinder><radius>0.15</radius><length>1</length></cylinder></geometry>
          <material><ambient>0.3 0.3 0.3 1</ambient></material>
        </visual>
      </link>
      <pose>1.5 1.0 0.5 0 0 0</pose>
    </model>

    <model name="pillar_b">
      <static>true</static>
      <link name="link">
        <collision name="collision">
          <geometry><cylinder><radius>0.15</radius><length>1</length></cylinder></geometry>
        </collision>
        <visual name="visual">
          <geometry><cylinder><radius>0.15</radius><length>1</length></cylinder></geometry>
          <material><ambient>0.3 0.3 0.3 1</ambient></material>
        </visual>
      </link>
      <pose>-1.5 -1.5 0.5 0 0 0</pose>
    </model>

  </world>
</sdf>
```

- [ ] **Step 2: 验证 world 能被 gazebo 解析**

Run（仅加载 world，快速退出，看有无致命错误）:
```bash
source /opt/ros/humble/setup.bash && source /home/jcp/p3_ws/install/setup.bash
colcon build --packages-select p3dx_slam --symlink-install
timeout 12 gazebo --verbose /home/jcp/p3_ws/install/p3dx_slam/share/p3dx_slam/worlds/p3dx_world.world 2>&1 | head -40
```

Expected: gazebo 启动，日志里无 `Error [parser.cc]` / `Unable to find uri[model://ground_plane]` 之类错误；`timeout` 到时自动退出（退出码 124 属正常）。

---

## Task 4: slam_toolbox 参数 + rviz 配置

**Files:**
- Create: `src/p3dx_slam/config/slam_toolbox_params.yaml`
- Create: `src/p3dx_slam/config/rviz_slam.rviz`

**Interfaces:**
- Consumes: 无（独立配置文件）
- Produces: `slam_toolbox_params.yaml` 顶层键 `slam_toolbox.ros__parameters`，含 `odom_frame`/`map_frame`/`base_frame`/`scan_topic`；`rviz_slam.rviz` 含 Map/LaserScan/RobotModel/TF/Grid 显示，Fixed Frame 为 `map`。

- [ ] **Step 1: 写 slam_toolbox_params.yaml**

```yaml
slam_toolbox:
  ros__parameters:
    # Solver
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_diagonal_type: BLOCK_DIAGONAL
    ceres_loss_function: None

    # Frames & topic
    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /scan

    # Map
    mode: mapping
    resolution: 0.05
    map_update_interval: 5.0
    max_laser_range: 12.0
    minimum_time_interval: 0.5
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000

    # Scan matching
    use_scan_matching: true
    use_scan_barycenter: true
    minimum_travel_distance: 0.5
    minimum_travel_heading: 0.5
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 1.5
    loop_search_maximum_distance: 3.0
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45
```

- [ ] **Step 2: 写 rviz_slam.rviz**

```yaml
Panels:
  - Class: rviz_common/Displays
    Name: Displays
  - Class: rviz_common/Views
    Name: Views
Visualization Manager:
  Class: ""
  Displays:
    - Class: rviz_default_plugins/Grid
      Name: Grid
      Enabled: true
      Normal Cell Count: 20
      Cell Size: 1.0
      Color: 125; 125; 125
      Plane: XY
      Plane Cell Count: 20
    - Class: rviz_default_plugins/Map
      Name: Map
      Enabled: true
      Topic:
        Value: /map
      Alpha: 0.7
    - Class: rviz_default_plugins/LaserScan
      Name: LaserScan
      Enabled: true
      Topic:
        Value: /scan
      Size (m): 0.05
      Color: 255; 0; 0
    - Class: rviz_default_plugins/RobotModel
      Name: RobotModel
      Enabled: true
      Description Topic:
        Value: /robot_description
      Visual Enabled: true
    - Class: rviz_default_plugins/TF
      Name: TF
      Enabled: true
      Frame Timeout: 15.0
  Global Options:
    Background Color: 48; 48; 48
    Fixed Frame: map
    Frame Rate: 30
  Tools:
    - Class: rviz_default_plugins/MoveCamera
    - Class: rviz_default_plugins/Select
    - Class: rviz_default_plugins/FocusCamera
    - Class: rviz_default_plugins/Measure
  Views:
    Current:
      Class: rviz_default_plugins/Orbit
      Name: Current View
      Distance: 8.0
      Focal Point:
        X: 0.0
        Y: 0.0
        Z: 0.5
      Pitch: 0.8
      Yaw: 0.0
```

- [ ] **Step 3: 验证 yaml 语法**

Run:
```bash
python3 -c "import yaml; d=yaml.safe_load(open('/home/jcp/p3_ws/src/p3dx_slam/config/slam_toolbox_params.yaml')); print('slam_toolbox keys ok:', 'scan_topic' in d['slam_toolbox']['ros__parameters']); yaml.safe_load(open('/home/jcp/p3_ws/src/p3dx_slam/config/rviz_slam.rviz')); print('rviz yaml ok')"
```

Expected: 输出两行 `... ok: True` 与 `rviz yaml ok`，无异常。

- [ ] **Step 4: 提交**

`p3dx_slam` 非 git 仓库，跳过 commit。

---

## Task 5: launch 文件 + 端到端验证

**Files:**
- Create: `src/p3dx_slam/launch/gazebo_slam.launch.py`

**Interfaces:**
- Consumes: Task 1 的 `pioneer3dx.xacro`（可解析）、Task 3 的 world、Task 4 的 yaml/rviz。
- Produces: 一键启动 gazebo + spawn 车 + rsp + jsp + slam_toolbox + rviz。启动后 `/scan`、`/cmd_vel`、`/odom`、`/map` 话题存在，`map→odom→base_link→lms100` TF 链完整。

- [ ] **Step 1: 写 gazebo_slam.launch.py**

```python
import os
import yaml

from ament_index_python.packages import get_package_share_directory
from launch import LaunchDescription
from launch.actions import IncludeLaunchDescription
from launch.launch_description_sources import PythonLaunchDescriptionSource
from launch.substitutions import Command
from launch_ros.actions import Node
from launch_ros.parameter_descriptions import ParameterValue


def generate_launch_description():
    p3dx_slam_dir = get_package_share_directory('p3dx_slam')
    p3dx_desc_dir = get_package_share_directory('p3dx_description_ros')
    gazebo_ros_dir = get_package_share_directory('gazebo_ros')

    world_path = os.path.join(p3dx_slam_dir, 'worlds', 'p3dx_world.world')
    xacro_path = os.path.join(p3dx_desc_dir, 'urdf', 'pioneer3dx.xacro')
    rviz_path = os.path.join(p3dx_slam_dir, 'config', 'rviz_slam.rviz')
    slam_params_path = os.path.join(p3dx_slam_dir, 'config', 'slam_toolbox_params.yaml')

    # 1. Gazebo（加载 world）
    gazebo = IncludeLaunchDescription(
        PythonLaunchDescriptionSource(
            os.path.join(gazebo_ros_dir, 'launch', 'gazebo.launch.py')
        ),
        launch_arguments={'world': world_path}.items(),
    )

    # 2. robot_description（xacro 生成）
    robot_description = ParameterValue(
        Command(['xacro ', xacro_path]), value_type=str
    )

    # 3. spawn 机器人到 gazebo
    spawn_entity = Node(
        package='gazebo_ros',
        executable='spawn_entity.py',
        arguments=['-topic', 'robot_description', '-entity', 'p3dx'],
        output='screen',
    )

    # 4. robot_state_publisher
    robot_state_publisher = Node(
        package='robot_state_publisher',
        executable='robot_state_publisher',
        parameters=[{
            'robot_description': robot_description,
            'use_sim_time': True,
        }],
        output='screen',
    )

    # 5. joint_state_publisher（发全 0 joint_states，让 rsp 发布完整 TF）
    joint_state_publisher = Node(
        package='joint_state_publisher',
        executable='joint_state_publisher',
        parameters=[{
            'robot_description': robot_description,
            'use_sim_time': True,
        }],
        output='screen',
    )

    # 6. slam_toolbox（online sync）
    with open(slam_params_path, 'r') as f:
        slam_params = yaml.safe_load(f)['slam_toolbox']['ros__parameters']
    slam_toolbox = Node(
        package='slam_toolbox',
        executable='sync_slam_toolbox_node',
        name='slam_toolbox',
        output='screen',
        parameters=[slam_params, {'use_sim_time': True}],
    )

    # 7. rviz2
    rviz = Node(
        package='rviz2',
        executable='rviz2',
        arguments=['-d', rviz_path],
        output='screen',
    )

    return LaunchDescription([
        gazebo,
        spawn_entity,
        robot_state_publisher,
        joint_state_publisher,
        slam_toolbox,
        rviz,
    ])
```

- [ ] **Step 2: 构建并启动**

Run:
```bash
source /opt/ros/humble/setup.bash && source /home/jcp/p3_ws/install/setup.bash
cd /home/jcp/p3_ws && colcon build --packages-select p3dx_slam --symlink-install
ros2 launch p3dx_slam gazebo_slam.launch.py
```

Expected: gazebo 窗口打开并加载 world + spawn 车；rviz 打开；终端无致命错误。

- [ ] **Step 3: 验证话题与 TF（另开终端）**

Run:
```bash
source /opt/ros/humble/setup.bash && source /home/jcp/p3_ws/install/setup.bash
ros2 topic list | grep -E '/scan|/cmd_vel|/odom|/map'
ros2 run tf2_ros tf2_echo map base_link
```

Expected: 四个话题都在；`tf2_echo` 输出 `map -> base_link` 的变换（Translation 初始接近 `[0,0,0]`）。

- [ ] **Step 4: 遥控建图验证**

在**第三个终端**运行:
```bash
source /opt/ros/humble/setup.bash && source /home/jcp/p3_ws/install/setup.bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

Expected: 用键盘 `i`（前进）等驱动车绕场，rviz 里 `Map` 逐渐铺满围墙轮廓，`/map` 话题持续更新 → 建图成功。

- [ ] **Step 5: 保存地图（可选验证）**

Run:
```bash
source /opt/ros/humble/setup.bash && source /home/jcp/p3_ws/install/setup.bash
ros2 run nav2_map_server map_saver_cli -f /tmp/p3dx_map
```

Expected: 生成 `/tmp/p3dx_map.pgm` 与 `/tmp/p3dx_map.yaml`（此步仅验证地图可导出，不属于本期 nav2 集成）。

- [ ] **Step 6: 提交**

`p3dx_slam` 非 git 仓库，跳过 commit。
